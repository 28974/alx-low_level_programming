#include "main.h"

/**
 * prints_numbers - function that prints the numbers, from 0 to 9
 *
 * Return: nothing
 */

void print_numbers(void)
{
	int x = 0;

	for (x = 0; x <= 9; x++)
	{
		_putchar('0' + i);
	}

	_putchar('\n');
}
